description: >
  Install gcov, lcov, coveralls-lcov

steps:
  - run:
      name: Install gcov, lcov, coveralls-lcov, gem
      command: |
         # sudo update-alternatives --install /usr/bin/gcov gcov /usr/bin/gcov-9 10;
         sudo apt install -y lcov rubygems;
         sudo gem install coveralls-lcov;
